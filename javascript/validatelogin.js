console.log("this is the profile script");
// JavaScript code in your web page

/*
function checkLoginStatus() {
    // Send an AJAX request to the PHP endpoint
    fetch('../php/loginendpoint.php')
        .then(response => response.json())
        .then(data => {
            if (data.loggedIn) {
                // User is logged in
                console.log('Logged in as ' + data.username);
                // You can show/hide elements or perform other actions accordingly
            } else {
                // User is not logged in
                console.log('Not logged in');
                // Redirect to the login page or display a login form
            }
        })
        .catch(error => {
            console.error('Error:', error);
        });
}

// Call the function to check login status when your page loads
window.addEventListener('load', checkLoginStatus);
*/